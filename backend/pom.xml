<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.peervault</groupId>
  <artifactId>peervault-backend</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>PeerVault Backend</name>
  <description>Zero-trust personal storage mesh — control plane microservices</description>

  <modules>
    <module>common</module>
    <module>eureka-server</module>
    <module>config-server</module>
    <module>api-gateway</module>
    <module>auth-service</module>
    <module>device-service</module>
    <module>file-service</module>
    <module>transfer-service</module>
    <module>security-service</module>
    <module>notification-service</module>
    <module>share-service</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.version>3.3.4</spring-boot.version>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
    <!-- Override the Boot BOM's pinned 1.18.34: the local JDK (26.0.1) is newer than that release
         supports, causing Lombok's javac-internals patching to silently no-op (annotation processing
         "succeeds" but emits no getters/setters/builders). 1.18.46 is the latest published release and
         includes fixes for newer JDKs. -->
    <lombok.version>1.18.46</lombok.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.peervault</groupId>
        <artifactId>common</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
          <!-- Not using spring-boot-starter-parent (only its BOM, imported above), so the repackage
               goal isn't auto-bound to the package phase the way it would be under that parent — bind it
               explicitly here once so every module that declares this plugin (all 9 apps; `common` never
               does) produces an executable fat jar, not just a plain thin one. -->
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
            <!-- Belt-and-braces alongside the lombok.version override above: explicitly wiring the
                 annotation processor path (rather than relying on javac's classpath auto-discovery of
                 optional dependencies) is what actually made Lombok work reliably on this JDK, confirmed
                 by transfer-service. Every module inherits this, so any future @Data/@Builder usage just
                 works without each service having to rediscover the same fix. -->
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <repositories>
    <repository>
      <id>spring-milestones</id>
      <name>Spring Milestones</name>
      <url>https://repo.spring.io/milestone</url>
    </repository>
  </repositories>

</project>
